import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pravintamilan.cyberracer',
  appName: 'Cyber Racer 3D',
  webDir: 'out'
};

export default config;
